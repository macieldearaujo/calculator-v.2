for(let i = 0; i <= 30; i++) {
    const par = i % 2 === 0 ? 'par' : 'impar';
    // console.log(`Número ${i} - ${par}`)
}

const frutas = ['maçã', 'laranja', 'limão', 'pêssego', 'lichia']

for(let i = 0; i <= frutas.length; i++) {
    console.log(`Índice ${i + 1}: ${frutas[i]}`)
}